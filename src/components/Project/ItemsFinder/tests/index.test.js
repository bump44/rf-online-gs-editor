// import React from 'react';
// import { shallow } from 'enzyme';

// import ProjectItemsFinder from '../index';

describe('<ProjectItemsFinder />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
