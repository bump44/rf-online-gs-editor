/*
 * ProjectItemsFinder Messages
 *
 * This contains all the text for the ProjectItemsFinder component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.ProjectItemsFinder.header',
    defaultMessage: 'This is the ProjectItemsFinder component !',
  },
});
