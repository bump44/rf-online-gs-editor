/*
 * ProjectResource Messages
 *
 * This contains all the text for the ProjectResource component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.ProjectResource.header',
    defaultMessage: 'This is the ProjectResource component !',
  },
});
