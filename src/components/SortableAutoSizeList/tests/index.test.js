// import React from 'react';
// import { shallow } from 'enzyme';

// import SortableAutoSizeList from '../index';

describe('<SortableAutoSizeList />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
