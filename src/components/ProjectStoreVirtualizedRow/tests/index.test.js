// import React from 'react';
// import { shallow } from 'enzyme';

// import ProjectStoreVirtualizedRow from '../index';

describe('<ProjectStoreVirtualizedRow />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
