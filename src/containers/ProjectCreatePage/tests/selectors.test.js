// import { fromJS } from 'immutable';
// import { selectProjectCreatePageDomain } from '../selectors';

describe('selectProjectCreatePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
