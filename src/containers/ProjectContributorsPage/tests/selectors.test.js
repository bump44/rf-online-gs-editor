// import { fromJS } from 'immutable';
// import { selectProjectContributorsPageDomain } from '../selectors';

describe('selectProjectContributorsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
