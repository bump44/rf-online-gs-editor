// import { fromJS } from 'immutable';
// import { selectProjectExportPageDomain } from '../selectors';

describe('selectProjectExportPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
