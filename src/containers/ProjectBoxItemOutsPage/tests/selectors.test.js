// import { fromJS } from 'immutable';
// import { selectProjectBoxItemOutsPageDomain } from '../selectors';

describe('selectProjectBoxItemOutsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
